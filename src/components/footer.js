import React from 'react';
import { Layout } from 'antd';

import './footer.css';

const Footer = (props) => {
  return (
    <Layout.Footer>
      Footer
    </Layout.Footer>
  );
};

export default Footer;
