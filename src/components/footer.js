import React from 'react';

import './footer.css';
import {Segment} from "semantic-ui-react";

const Footer = (props) => {
  return (
    <Segment>
      Footer
    </Segment>
  );
};

export default Footer;
